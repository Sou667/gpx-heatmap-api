# Web-Framework & Server
flask>=2.2.2
gunicorn>=20.1.0

# HTTP-Client
requests>=2.28.0

# Karten- und Geo-Funktionen
folium>=0.12.1
geopy>=2.2.0
gpxpy>=1.4.2

# Astronomie-/Wetterdaten (für Sonnenauf- und -untergang)
astral>=3.2

# PDF-Rendering (falls benötigt – z. B. für zusätzliches Reporting)
weasyprint>=53.3
